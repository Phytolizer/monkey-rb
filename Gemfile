# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'test-unit'
gem 'typesafe_enum', git: 'https://github.com/dmolesUC/typesafe_enum'
